pipeline {
    agent any

    stages {
        stage('Deploy to Node VM') {
            steps {
                sh '''
                ssh -o StrictHostKeyChecking=no ubuntu@44.223.36.219 "
                    cd /home/ubuntu/simple-nodejs-app &&
                    git pull origin main &&
                    npm install &&
                    pm2 restart nodeapp || pm2 start index.js --name nodeapp
                "
                '''
            }
        }
    }
}
