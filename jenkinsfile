pipeline {
    agent any

    stages {
        stage('Deploy to Node VM') {
            steps {
                sh '''
                ssh -o StrictHostKeyChecking=no ubuntu@44.223.92.147 "
                    cd /home/ubuntu/app &&
                    git pull origin main &&
                    npm install &&
                    pm2 restart nodeapp || pm2 start index.js --name nodeapp
                "
                '''
            }
        }
    }
}
